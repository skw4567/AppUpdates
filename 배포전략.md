# the PLUS Piping 배포 및 DB 관리 전략 가이드

## 1. 자동 업데이트 (GitHub 연동)
* **서버 구성**: GitHub Private Repo (`skw4567/AppUpdates`) 활용.
* **업데이트 로직**: `manifest.json`을 읽어 파일별 SHA256 해시값 비교 후 변경된 파일만 다운로드.
* **설정 관리**: `updater_config.json`에 GitHub Token 및 리포지토리 정보 집중화 (소스 수정 불필요).
* **안전 장치**: 업데이트 중 `.bak` 백업 생성 및 실패 시 자동 롤백.

## 2. DB 배포 및 네트워크 구성 (3~5대 협업 환경)
* **배포 모델**: PostgreSQL 무설치판(Portable)을 패키지에 포함.
* **자동 서비스 등록**: NSIS 설치 시 `pg_ctl register`를 실행하여 윈도우 서비스로 자동 등록. 
  - *이점*: 메인 프로그램 종료 시에도 DB 서버는 백그라운드에서 유지되어 다른 작업자 접속 가능.
* **Zero-Config**: 사용자가 IP/PW를 몰라도 설치 시 자동으로 DB 초기화 및 방화벽(5432 포트) 허용 설정.
* **책임 분리**: 데이터 저장 경로(Data Folder)를 업체가 직접 선택하게 유도하여 데이터 파손 책임 소재 명확화.

## 3. DB 연결 리팩토링 원칙
* **연결(Connection)**: 프로그램 실행 중 Singleton 또는 클래스 멤버로 유지 (성능 및 WAL 모드 최적화).
* **커서(Cursor)**: 쿼리 실행 직후 반드시 `close()` 처리 (메모리 누수 방지). `with` 문 사용 권장.
* **추상화**: `DatabaseManager`를 통해 SQLite와 PostgreSQL을 동적으로 선택할 수 있는 구조 구축.

# ===========================================

# 🚀 윈도우 서비스(Windows Service) 방식 배포 전략

3~5대의 컴퓨터가 협업하는 소규모 네트워크 환경에서, 메인 PC의 프로그램 종료 여부와 상관없이 데이터베이스(PostgreSQL)를 안정적으로 유지하기 위한 배포 전략입니다.

## 1. 핵심 컨셉: "배경 실행(Background Execution)"
* **문제점**: 임베디드(부모-자식) 방식으로 DB를 띄우면 메인 사용자가 프로그램을 종료할 때 DB도 같이 죽어 타사 작업자가 접속할 수 없음.
* **해결책**: PostgreSQL을 윈도우 시스템의 **'서비스(Service)'**로 등록하여 컴퓨터 전원만 켜져 있으면 상시 구동되도록 설정.

## 2. 사용자 경험 (Zero-Config UI)
사용자에게 복잡한 설정을 요구하지 않도록 NSIS 설치 프로그램에서 다음 과정을 자동화합니다.

### A. 설치 모드 선택 (NSIS)
1. **메인 서버용**: DB 엔진 설치 + 서비스 등록 + 방화벽 허용.
2. **작업자 전용**: DB 엔진 미설치 + 서버 IP 연결 설정만 수행.

### B. 자동화 프로세스 (사용자 몰래 실행)
1. **파일 복사**: PostgreSQL 바이너리를 프로그램 폴더 내 `pgsql/`에 복사.
2. **DB 초기화**: `initdb.exe` 실행으로 데이터 저장 폴더 생성.
3. **서비스 등록**: `pg_ctl.exe register` 명령으로 윈도우 서비스에 강제 등록.
4. **포트 개방**: `netsh` 명령으로 5432 포트 방화벽 예외 등록.

## 3. 기술적 구현 명령어 (NSIS 스크립트용)

### 서비스 등록 예시
```bash
# -N: 서비스 이름, -D: 데이터 폴더 경로, -o: 추가 옵션(포트 등), -t: 시작 유형(auto)
pg_ctl.exe register -N "thePLUS_Piping_DB" -D "C:\PipingData" -o "-p 5432" -t auto
```

# 🛠️ 자동 서비스 등록 메커니즘 (NSIS 활용)
설치 프로그램이 실행될 때 다음과 같은 과정이 사용자 몰래(Silent) 진행됩니다.

1. 파일 복사: PostgreSQL 무설치 바이너리(bin, lib 등)를 C:\Program Files\the PLUS Piping\pgsql 폴더에 복사합니다.

2. DB 초기화: initdb.exe를 실행하여 데이터가 저장될 data 폴더를 생성합니다. (이때 비밀번호나 포트 설정을 자동으로 주입합니다.)

3. 서비스 등록: NSIS 스크립트에서 아래와 같은 명령어를 실행합니다.
```bash
pg_ctl.exe register -N "thePLUS_DB" -D "C:\data" -o "-p 5432" -t auto
```
  * 이 명령 한 줄이면 윈도우의 '서비스' 목록에 등록되며, 컴퓨터가 켜질 때마다 자동으로 실행됩니다.

4. 방화벽 해제: netsh advfirewall 명령어를 실행하여 5432 포트를 열어줍니다. 그래야 옆에 있는 3~5대의 컴퓨터가 이 서버에 접속할 수 있습니다.

# 🛡️ 데이터 파손 책임 회피 전략 (Zero-Config형)
컴퓨터를 모르는 사용자들에게 책임을 지우기 위해, 설치 시 다음과 같은 장치를 마련할 수 있습니다.

* 데이터 폴더 위치 선택: 설치 중에 "데이터가 저장될 안전한 드라이브를 선택하세요"라고 창을 띄웁니다. 사용자가 D:\Piping_Data를 선택하면, 서비스는 그곳을 바라보게 설정됩니다.

* 제거 시 주의사항: 프로그램을 삭제(Uninstall)할 때, **"데이터 파일도 삭제하시겠습니까?"**라고 묻고 기본값을 **"아니오"**로 둡니다. "데이터를 남겨두어야 나중에 복구가 가능합니다"라는 경고를 곁들입니다.

# 📋 요약: 개발자인 당신이 할 일
이 구조로 가면 사용자님은 소스 코드를 수정할 필요가 거의 없습니다. 대신 NSIS 스크립트에 다음 로직을 추가하는 데 집중하시면 됩니다.

1. PostgreSQL 무설치판을 패키지에 포함하기.

2. 설치 시 pg_ctl register 명령어를 관리자 권한으로 실행하기.

3. 설치 시 윈도우 방화벽 규칙에 5432 포트 허용 추가하기.

이렇게 배포하면 사용자는 **"그냥 인스톨러 하나 깔았더니 우리 사무실 5대가 같이 쓸 수 있게 됐다"**라고 느끼게 됩니다. 최고의 사용자 경험이죠.

이 NSIS 스크립트에서 PostgreSQL을 서비스로 등록하고 초기화하는 구체적인 명령어 코드가 필요하신가요? 작성해 드릴 수 있습니다.
